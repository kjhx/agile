name: Merge
on:
  push:
    branches: [main]

jobs:
  deploy:
    name: Deploy (PROD)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@25a956c
        run: vercel -n 'agile' -c --prod -t ${{ secrets.VERCEL_TOKEN }}
